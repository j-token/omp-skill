---
name: text-case
description: Use when the user asks for deterministic uppercase or lowercase conversion through the example Pi package.
---

# Text case

Use the `text_case` tool for exact uppercase or lowercase conversion. Preserve the input characters except for case conversion, select the requested `upper` or `lower` action, and return the tool result without manually rewriting the text.
