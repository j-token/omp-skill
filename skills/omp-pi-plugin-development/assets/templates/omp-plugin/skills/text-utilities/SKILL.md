---
name: text-utilities
description: Use when the user asks to count words or perform deterministic text utility operations supplied by the example OMP plugin.
---

# Text utilities

Use the `word_count` tool when the user asks for an exact word count. Pass only the text to count, preserve the original text, and report the returned integer without estimating it yourself.
